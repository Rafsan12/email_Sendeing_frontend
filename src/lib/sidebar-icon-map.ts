import { LayoutDashboard, Mail, Plus, Users } from "lucide-react";

export const sidebarIconMap: Record<string, React.ElementType> = {
  mail: Mail,
  plus: Plus,
  users: Users,
  dashboard: LayoutDashboard,
};
